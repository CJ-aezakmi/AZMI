# 🚀 AEZAKMI Launcher

Электронный лаунчер для AEZAKMI Antidetect Browser, который автоматически:
- ✅ Проверяет наличие установленного AEZAKMI
- ✅ Скачивает и устанавливает последнюю версию с GitHub
- ✅ Проверяет обновления при каждом запуске
- ✅ Устанавливает Node.js 20 portable
- ✅ Устанавливает Playwright с Chromium
- ✅ Настраивает окружение для работы приложения

## 📦 Для пользователя

### Первая установка:

1. Скачай **AEZAKMI Launcher Setup.exe** из [Releases](https://github.com/CJ-aezakmi/AZMI/releases)
2. Запусти установщик лаунчера
3. Запусти **AEZAKMI Launcher**
4. Подожди пока лаунчер:
   - Скачает и установит AEZAKMI (~50 MB)
   - Скачает Node.js (~50 MB)
   - Установит Playwright и Chromium (~300 MB)
5. AEZAKMI запустится автоматически!

### Последующие запуски:

- Просто запускай **AEZAKMI Launcher**
- Лаунчер проверит обновления
- Установит их если есть
- Запустит AEZAKMI

## 🔧 Архитектура

```
%LOCALAPPDATA%\AEZAKMI\
├── app\                          # Установленное приложение
│   ├── AEZAKMI.exe
│   ├── resources\
│   └── node_modules -> ../runtime/node_modules (symlink)
└── runtime\                      # Разделяемые зависимости
    ├── node\                     # Node.js 20 portable
    │   ├── node.exe
    │   ├── npm.cmd
    │   └── npx.cmd
    ├── node_modules\             # Playwright и зависимости
    │   └── playwright\
    └── ms-playwright\            # Chromium браузер
        └── chromium-*/
```

### Преимущества:

1. **Лёгкий основной установщик**: ~50MB вместо ~400MB
2. **Быстрые обновления**: Скачивается только изменившийся код
3. **Разделяемые зависимости**: Node.js и Playwright общие для всех версий
4. **Автоматическая проверка обновлений**: Всегда актуальная версия
5. **Простая установка**: Один .exe файл делает всё

## 🛠️ Для разработчика

### Разработка:

```powershell
cd launcher
npm install
npm start
```

### Сборка:

```powershell
cd launcher
npm run build
```

Установщик появится в `launcher\dist\AEZAKMI Launcher Setup.exe`

### Автоматическая сборка:

GitHub Actions автоматически собирает лаунчер при каждом пуше:
- **Tauri App**: `AEZAKMI_x64-setup.exe` (~50MB)
- **Launcher**: `AEZAKMI Launcher Setup.exe` (~5MB)

### Как это работает:

1. **Лаунчер** (`main.js`):
   - Проверяет установлен ли AEZAKMI
   - Если нет - скачивает с GitHub Releases
   - Проверяет обновления (сравнивает версии)
   - Проверяет Node.js в `%LOCALAPPDATA%\AEZAKMI\runtime\node`
   - Если нет - скачивает Node.js 20 portable
   - Проверяет Playwright в `%LOCALAPPDATA%\AEZAKMI\runtime\node_modules`
   - Если нет - устанавливает через npm
   - Проверяет Chromium в `%LOCALAPPDATA%\AEZAKMI\runtime\ms-playwright`
   - Если нет - скачивает через `playwright install chromium`
   - Создаёт symlink `node_modules` в папке приложения
   - Запускает AEZAKMI с правильными переменными окружения

2. **AEZAKMI App**:
   - Использует Node.js из `runtime/node`
   - Использует Playwright из `runtime/node_modules`
   - Использует Chromium из `runtime/ms-playwright`

## 📊 Структура проекта:

```
launcher/
├── package.json          # Конфигурация Electron + electron-builder
├── main.js              # Основная логика лаунчера (Electron main process)
├── index.html           # UI лаунчера (красивое окно с прогрессом)
└── icon.ico             # Иконка (заглушка, можно заменить)
```

## 🎨 Кастомизация:

### Изменить репозиторий:

```javascript
// main.js, строка 11
const GITHUB_REPO = 'CJ-aezakmi/AZMI';
```

### Изменить пути установки:

```javascript
// main.js, строки 12-14
const INSTALL_DIR = path.join(process.env.LOCALAPPDATA, 'AEZAKMI', 'app');
const RUNTIME_DIR = path.join(process.env.LOCALAPPDATA, 'AEZAKMI', 'runtime');
```

### Изменить версию Node.js:

```javascript
// main.js, строка 15
const NODE_VERSION = 'v20.11.1';
```

### Изменить UI:

Отредактируй `index.html` - обычный HTML с CSS (градиент, прогресс-бар, спиннер)

## 🔍 Отладка:

Лаунчер логирует всё в консоль:

```javascript
console.log('[Launcher] Current: 2.0.0, Latest: 2.0.3');
console.log('[Launcher] Playwright install completed');
console.log('[Launcher] App launched successfully');
```

Запусти в режиме разработки чтобы видеть консоль:
```powershell
npm start
```

## 📝 Решение проблем:

### "Не удалось установить"
- Проверь права администратора
- Проверь антивирус (добавь в исключения)

### "Приложение не найдено после установки"
- Проверь `CANDIDATE_EXES` в `main.js`
- Убедись что NSIS установщик создаёт файл в правильном месте

### "Ошибка скачивания Node.js"
- Проверь интернет-соединение
- Проверь доступность nodejs.org
- Можно изменить зеркало в `NODE_URL`

### "Ошибка установки Playwright"
- Убедись что Node.js установлен
- Проверь `npm` работает: `node -v && npm -v`

## 🚀 Деплой:

1. **Локальная сборка**:
   ```powershell
   cd launcher
   npm run build
   ```
   
2. **Загрузка в Release**:
   - Вручную: Загрузи `dist/*.exe` в GitHub Releases
   - Автоматически: Workflow делает это при создании тега

3. **Создание релиза**:
   ```powershell
   git tag v2.0.4
   git push --tags
   ```
   
   Workflow автоматически соберёт и загрузит оба установщика

## 📖 Inspiration:

Основано на лаунчере из проекта [UKULUT/Antic Browser](https://github.com/CJ-aezakmi/UKULUT) ❤️
