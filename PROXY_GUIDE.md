# 🌐 Полное руководство по работе с прокси в AEZAKMI Pro

## 📋 Содержание
1. [Поддерживаемые типы прокси](#поддерживаемые-типы-прокси)
2. [Форматы ввода прокси](#форматы-ввода-прокси)
3. [Как работает авторизация](#как-работает-авторизация)
4. [Пошаговая настройка](#пошаговая-настройка)
5. [Диагностика проблем](#диагностика-проблем)
6. [Технические детали](#технические-детали)

---

## 🔌 Поддерживаемые типы прокси

### ✅ HTTP/HTTPS Proxy
- **С авторизацией**: ✅ Полная поддержка
- **Без авторизации**: ✅ Полная поддержка
- **Использование**: Обычные прокси для веб-трафика
- **Порты по умолчанию**: 8080, 3128, 80

### ✅ SOCKS5 Proxy
- **С авторизацией**: ✅ Полная поддержка
- **Без авторизации**: ✅ Полная поддержка
- **Использование**: Универсальный протокол для любого TCP трафика
- **Порты по умолчанию**: 1080, 1081

### ⚠️ SOCKS4 Proxy
- **С авторизацией**: ⚠️ Ограниченная поддержка (без пароля)
- **Без авторизации**: ✅ Полная поддержка
- **Использование**: Устаревший протокол
- **Порты по умолчанию**: 1080

---

## 📝 Форматы ввода прокси

### 1. URL формат (рекомендуется)
```
protocol://username:password@host:port
```

**Примеры:**
```
http://user:pass@123.45.67.89:8080
https://mylogin:mypass@proxy.example.com:3128
socks5://user123:password123@socks.example.com:1080
```

### 2. Компактный формат с @
```
username:password@host:port
```

**Примеры:**
```
user:pass@123.45.67.89:8080
mylogin:mypass@proxy.example.com:3128
```
*(Автоматически определяется как HTTP прокси)*

### 3. Формат с двоеточиями
```
host:port:username:password
```

**Примеры:**
```
123.45.67.89:8080:user:pass
proxy.example.com:3128:mylogin:mypass
```

### 4. Без авторизации
```
host:port
```

**Примеры:**
```
123.45.67.89:8080
proxy.example.com:3128
socks5://socks.example.com:1080
```

---

## 🔐 Как работает авторизация

### HTTP/HTTPS с авторизацией
AEZAKMI использует **локальный туннельный прокси-сервер** для обработки авторизации:

```
Браузер → Локальный туннель (127.0.0.1:случайный порт)
                    ↓
         Добавление Basic Auth заголовка
                    ↓
         Реальный прокси с авторизацией
                    ↓
         Целевой сайт
```

**Преимущества:**
- ✅ 100% совместимость с Chromium
- ✅ Нет ограничений на длину пароля
- ✅ Работает со спецсимволами в пароле
- ✅ Не требует расширений браузера

### SOCKS5 с авторизацией
Используется нативная SOCKS5 авторизация через библиотеку `socks`:

```
Браузер → Локальный HTTP туннель (127.0.0.1:случайный порт)
                    ↓
         SOCKS5 подключение с username/password
                    ↓
         Реальный SOCKS5 сервер
                    ↓
         Целевой сайт
```

**Преимущества:**
- ✅ Полная поддержка SOCKS5 авторизации
- ✅ Работает с любыми провайдерами
- ✅ Более быстрая работа чем HTTP

---

## 🚀 Пошаговая настройка

### Способ 1: Быстрый ввод

1. Откройте **"Создать профиль"**
2. Перейдите на вкладку **"Прокси"**
3. Выберите режим **"Быстрый ввод"**
4. Вставьте прокси в любом формате:
   ```
   http://user:pass@123.45.67.89:8080
   ```
5. Нажмите **"Применить"**
6. Нажмите **"Сохранить"**

### Способ 2: Ручной ввод

1. Откройте **"Создать профиль"**
2. Перейдите на вкладку **"Прокси"**
3. Выберите режим **"Ручной ввод"**
4. Заполните поля:
   - ✅ Использовать прокси
   - **Тип**: HTTP / HTTPS / SOCKS5
   - **Хост**: `123.45.67.89`
   - **Порт**: `8080`
   - **Логин**: `user` (опционально)
   - **Пароль**: `pass` (опционально)
5. Нажмите **"Сохранить"**

### Способ 3: Выбор из списка

1. Сначала добавьте прокси в раздел **"Прокси"** (боковое меню)
2. При создании профиля выберите режим **"Выбор из списка"**
3. Выберите нужный прокси из выпадающего списка
4. Нажмите **"Сохранить"**

---

## 🔍 Диагностика проблем

### ❌ Профиль не запускается

**Симптомы:**
- Браузер не открывается
- Ошибка в логах: "Proxy connection failed"

**Решение:**
1. Проверьте формат прокси - используйте URL формат
2. Убедитесь что логин/пароль не содержат спецсимволов без экранирования
3. Проверьте что прокси работает (используйте проверку в разделе "Прокси")

### ❌ Браузер открывается, но не загружает сайты

**Симптомы:**
- Браузер запускается
- Страницы не загружаются
- Ошибки "ERR_TUNNEL_CONNECTION_FAILED"

**Решение:**
1. Откройте консоль разработчика в приложении (F12 в dev режиме)
2. Найдите строки `[PUPPETEER]` или `[TUNNEL]` в логах
3. Проверьте что username и password передаются корректно
4. Убедитесь что прокси сервер не блокирует соединения

### ❌ Прокси с авторизацией не работает

**Симптомы:**
- Получаете ошибку "407 Proxy Authentication Required"
- Логи показывают: "Proxy returned: 407"

**Решение:**
1. **Проверьте пробелы**: логин и пароль не должны содержать пробелов
2. **Проверьте спецсимволы**: если пароль содержит `@`, `:`, `/` - используйте URL encoding:
   ```
   Пароль: p@ss:word/123
   Encoded: p%40ss%3Aword%2F123
   ```
3. **Длина пароля**: некоторые прокси ограничивают длину - попробуйте короткий пароль
4. **Попробуйте другой тип**: если HTTP не работает, попробуйте SOCKS5

### ❌ SOCKS5 не подключается

**Симптомы:**
- Ошибка "Socks5 Authentication failed"
- Таймаут подключения

**Решение:**
1. Убедитесь что SOCKS5 сервер поддерживает авторизацию
2. Проверьте версию протокола - используйте `socks5://` а не `socks://`
3. Некоторые SOCKS5 серверы требуют username но не password - оставьте пароль пустым

---

## 🔧 Технические детали

### Архитектура системы прокси

```
Frontend (React/TypeScript)
    ↓
src/lib/launchProfile.ts - формирование payload
    ↓
Tauri Backend (Rust)
    ↓
src-tauri/src/lib.rs - запуск Node.js процесса
    ↓
scripts/launch_puppeteer.cjs - запуск браузера
    ↓
createLocalProxyTunnel() - создание туннеля
    ↓
Puppeteer с локальным прокси
    ↓
Chromium браузер
```

### Структура данных прокси

**TypeScript (Frontend):**
```typescript
interface Proxy {
  enabled: boolean;
  type: 'http' | 'https' | 'socks5' | 'socks4';
  host: string;
  port: string | number;
  username?: string;
  login?: string;  // Для совместимости
  password?: string;
  status?: 'working' | 'failed' | 'unchecked';
}
```

**JSON Payload для Puppeteer:**
```javascript
{
  profileName: "Мой профиль",
  profileDir: "aezakmi-profile-1234567890",
  userDataDir: "aezakmi-profile-1234567890",
  args: ["--no-sandbox", ...],
  url: "https://whoer.net",
  proxy: {
    server: "http://123.45.67.89:8080",
    username: "user",
    password: "pass"
  }
}
```

### Логи для отладки

**Включение логов в production:**
1. Откройте DevTools (если доступно)
2. Перейдите в консоль
3. Ищите префиксы:
   - `[LAUNCH]` - фронтенд формирует payload
   - `[PROD]` - Rust бэкенд
   - `[PUPPETEER]` - Puppeteer скрипт
   - `[TUNNEL]` - Локальный прокси-туннель

**Пример успешного запуска:**
```
[LAUNCH] Запуск профиля: Test Profile
[LAUNCH] Прокси настроен: { server: 'http://...', hasAuth: true }
[PROD] ✓ Node.js: ...
[PROD] ✓ Скрипт: ...
[PROD] ✓ Процесс запущен с PID: 12345
[PUPPETEER] Загружен Puppeteer с полным антидетектом
[PUPPETEER] 🔧 Настройка прокси: http://123.45.67.89:8080
[PUPPETEER] 🚇 Создание локального туннеля для авторизации...
[TUNNEL] ✅ Локальный прокси слушает на порту: 54321
[PUPPETEER] ✅ Локальный туннель запущен на порту: 54321
[PUPPETEER] 🚀 Запуск браузера...
[PUPPETEER] ✅ Браузер запущен
[PUPPETEER] 📄 Переход на: https://whoer.net
[PUPPETEER] ✅ Страница загружена
[PUPPETEER] ✅ Браузер работает
```

### Проверка прокси через whoer.net

После запуска профиля браузер автоматически открывает `whoer.net`, где вы можете проверить:

- ✅ **IP адрес** - должен соответствовать прокси
- ✅ **Местоположение** - должно соответствовать прокси
- ✅ **Anonymity** - должно быть высоким
- ✅ **WebRTC** - должен быть заблокирован (если включено в настройках)

---

## 🎯 Рекомендации

### Лучшие практики

1. **Используйте URL формат** для ввода прокси - это самый надёжный способ
2. **Проверяйте прокси перед использованием** в разделе "Прокси"
3. **Для SOCKS5 используйте `socks5://` префикс** явно
4. **Избегайте спецсимволов в паролях** или используйте URL encoding
5. **Сохраняйте прокси в список** для быстрого переиспользования

### Выбор типа прокси

| Задача | Рекомендуемый тип |
|--------|-------------------|
| Обычный веб-серфинг | HTTP/HTTPS |
| Работа с API | HTTP/HTTPS |
| P2P приложения | SOCKS5 |
| Максимальная скорость | SOCKS5 |
| Обход блокировок | SOCKS5 |
| Старые прокси | SOCKS4 |

### Проблемные провайдеры

Некоторые прокси-провайдеры имеют особенности:

- **Luminati/BrightData**: используйте HTTP, формат `username-session-xxx:password`
- **Oxylabs**: используйте HTTP, может потребоваться whitelist IP
- **Smartproxy**: используйте HTTP, поддержка ротации через username
- **SOCKS прокси от VPN**: часто без авторизации, используйте как `host:port`

---

## 💡 FAQ

**Q: Можно ли использовать прокси без авторизации?**  
A: Да, просто оставьте поля логин/пароль пустыми или используйте формат `host:port`

**Q: Поддерживается ли IPv6?**  
A: Да, используйте квадратные скобки: `http://[2001:db8::1]:8080`

**Q: Как использовать прокси с ротацией?**  
A: Настройте ротацию через username прокси-провайдера (например, `user-session-xxxxx`)

**Q: Работает ли с резидентными прокси?**  
A: Да, все типы прокси поддерживаются

**Q: Можно ли использовать Tor?**  
A: Да, используйте `socks5://127.0.0.1:9050` (по умолчанию Tor работает без авторизации)

**Q: Почему whoer.net показывает мой реальный IP?**  
A: Проверьте что прокси настроен правильно и работает. Также проверьте логи на ошибки подключения.

---

## 📞 Поддержка

Если у вас остались вопросы или проблемы:

1. Проверьте логи в консоли (DevTools)
2. Убедитесь что прокси работает через внешние сервисы
3. Попробуйте другой формат ввода прокси
4. Создайте Issue на GitHub с логами

---

**Дата обновления**: 03 февраля 2026  
**Версия документа**: 1.0  
**Версия AEZAKMI Pro**: 2.0.0
