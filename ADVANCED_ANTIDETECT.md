# 🛡️ Продвинутая Система Антидетекта AEZAKMI Pro

## Обзор

AEZAKMI Pro теперь использует продвинутую систему антидетекта, вдохновленную архитектурой **donutbrowser** и **Camoufox**. Система обеспечивает максимальную защиту от современных методов детекции браузерных отпечатков.

## 🎯 Ключевые Улучшения

### 1. **Множественные Браузерные Движки**
- ✅ **Chromium** - стандартный Chrome-based браузер
- ✅ **Firefox** - Mozilla Firefox со встроенной защитой
- ✅ **Camoufox** (🔥 рекомендуется) - модифицированный Firefox с нативным антидетектом
- ✅ **WebKit** - Safari engine (macOS/Linux)

### 2. **Реалистичная Генерация Фингерпринтов**

Вместо случайных параметров, система использует:
- 📊 **Статистические модели** реальных браузеров
- 🎲 **Bayesian Networks** для корреляции параметров
- 🌍 **Географическая привязка** к прокси
- 🖥️ **OS-специфичные конфигурации**

### 3. **Продвинутые Техники Антидетекта**

#### Canvas Fingerprinting Protection
```javascript
// Добавляет незаметный шум к canvas операциям
const noise = 0.1; // Минимальный, незаметный глазу
imageData.data[i] += Math.random() * noise;
```

#### WebGL Fingerprinting Protection
```javascript
// Подменяет vendor/renderer на реалистичные значения
UNMASKED_VENDOR_WEBGL: 'Intel Inc.'
UNMASKED_RENDERER_WEBGL: 'Intel Iris OpenGL Engine'
```

#### Audio Context Fingerprinting
```javascript
// Добавляет микро-шум к аудио анализу
analyser.getFloatFrequencyData = function(array) {
  for (let i = 0; i < array.length; i++) {
    array[i] += (Math.random() - 0.5) * 0.001;
  }
};
```

#### WebRTC Leak Protection
- Полная блокировка RTCPeerConnection
- Защита от утечки реального IP
- Замена local/public IP адресов

### 4. **Улучшенная Работа с Прокси**

#### Автоматическое Туннелирование
```
Браузер → Локальный туннель (127.0.0.1:random) → Прокси с auth → Интернет
```

**Преимущества:**
- ✅ Работает с любым типом прокси
- ✅ Поддержка SOCKS5/SOCKS4 с авторизацией
- ✅ Автоматическое управление credentials
- ✅ Нет утечки авторизации в браузер

#### Поддерживаемые Форматы
```
http://user:pass@host:port
socks5://user:pass@host:port
user:pass@host:port
host:port:user:pass
host:port
```

## 📋 Настройка Профиля

### Базовые Параметры
```typescript
{
  name: "Профиль 1",
  browserEngine: "camoufox",  // chromium | firefox | camoufox
  os: "windows",              // windows | macos | linux
  userAgent: "auto",          // Автогенерация или кастомный
  screenWidth: 1920,
  screenHeight: 1080,
}
```

### Антидетект Конфигурация
```typescript
{
  antidetect: {
    // Базовые опции
    canvasNoise: true,        // Защита от Canvas fingerprinting
    webglNoise: true,         // Защита от WebGL fingerprinting
    audioNoise: true,         // Защита от Audio fingerprinting
    blockWebRTC: true,        // Блокировка WebRTC
    
    // Расширенные опции
    canvas: {
      noise: true,
      noiseLevel: 0.1,        // 0.1-1.0, чем больше - тем заметнее
    },
    
    webgl: {
      noise: true,
      vendor: "Intel Inc.",
      renderer: "Intel Iris OpenGL Engine",
    },
    
    audio: {
      noise: true,
      contextNoise: 0.001,    // Микро-шум для AudioContext
    },
    
    webrtc: {
      block: true,
      replacePublicIP: true,
      replaceLocalIP: true,
    },
    
    // Hardware характеристики
    hardwareConcurrency: 8,   // Количество CPU ядер
    deviceMemory: 8,          // GB памяти
    
    // Дополнительная защита
    hideAutomation: true,     // Скрыть navigator.webdriver
    spoofPlugins: true,       // Подменить список плагинов
    spoofBattery: true,       // Подменить Battery API
  }
}
```

### Прокси Конфигурация
```typescript
{
  proxy: {
    enabled: true,
    type: "socks5",           // http | https | socks5 | socks4
    host: "proxy.example.com",
    port: "1080",
    username: "user",
    password: "pass",
  }
}
```

## 🚀 Использование

### 1. Создание Профиля

```typescript
import { Profile } from '@/types';

const profile: Profile = {
  id: crypto.randomUUID(),
  name: "Безопасный профиль",
  browserEngine: "camoufox",
  os: "windows",
  userAgent: "auto", // Автогенерация на основе OS
  language: "ru-RU",
  screenWidth: 1920,
  screenHeight: 1080,
  timezone: "Europe/Moscow",
  proxy: {
    enabled: true,
    type: "socks5",
    host: "proxy.com",
    port: "1080",
    username: "user",
    password: "pass",
  },
  antidetect: {
    canvasNoise: true,
    webglNoise: true,
    audioNoise: true,
    blockWebRTC: true,
    spoofGeolocation: false,
  },
  status: "ready",
  createdAt: new Date().toISOString(),
};
```

### 2. Запуск Профиля

```typescript
import { launchProfile } from '@/lib/launchProfile';

await launchProfile(profile);
```

### 3. Проверка Антидетекта

Откройте в профиле:
- 🔍 [whoer.net](https://whoer.net) - общая проверка
- 🔍 [browserleaks.com](https://browserleaks.com) - детальная проверка
- 🔍 [pixelscan.net](https://pixelscan.net) - продвинутая проверка

## 🎨 Рекомендуемые Настройки

### Для Максимальной Анонимности
```typescript
{
  browserEngine: "camoufox",
  antidetect: {
    canvasNoise: true,
    webglNoise: true,
    audioNoise: true,
    blockWebRTC: true,
    hideAutomation: true,
    spoofPlugins: true,
    spoofBattery: true,
  }
}
```

### Для Работы с Социальными Сетями
```typescript
{
  browserEngine: "camoufox",
  antidetect: {
    canvasNoise: true,
    webglNoise: true,
    audioNoise: false,      // Может влиять на звонки
    blockWebRTC: false,     // Нужен для видеозвонков
    hideAutomation: true,
  }
}
```

### Для E-commerce / Парсинга
```typescript
{
  browserEngine: "chromium",
  antidetect: {
    canvasNoise: true,
    webglNoise: true,
    audioNoise: true,
    blockWebRTC: true,
    hideAutomation: true,
    hardwareConcurrency: 4,  // Более распространенное значение
    deviceMemory: 8,
  }
}
```

## 🔬 Технические Детали

### Как Работает Canvas Protection

Canvas fingerprinting - один из самых точных методов идентификации браузера. Каждая система рендерит графику немного по-разному из-за различий в GPU, драйверах, шрифтах.

**Наша защита:**
1. Перехватываем `toDataURL()`, `toBlob()`, `getImageData()`
2. Добавляем незаметный шум (0.1 пикселя) к изображению
3. Шум меняется при каждом рендере, но незаметен глазу
4. Фингерпринт меняется, но изображение выглядит идентично

### Как Работает WebGL Protection

WebGL предоставляет детальную информацию о GPU через `UNMASKED_VENDOR_WEBGL` и `UNMASKED_RENDERER_WEBGL`.

**Наша защита:**
1. Перехватываем `getParameter()`
2. Возвращаем реалистичные значения вместо реальных
3. Используем статистику самых распространенных GPU
4. Подбираем GPU под OS (Intel для Windows/Linux, Apple для macOS)

### Как Работает Audio Protection

AudioContext API позволяет создавать уникальный отпечаток через анализ частот.

**Наша защита:**
1. Перехватываем `getFloatFrequencyData()`
2. Добавляем микро-шум (0.001) к частотным данным
3. Шум не влияет на восприятие звука
4. Фингерпринт меняется при каждом анализе

### Как Работает Proxy Tunnel

Многие прокси требуют Basic Auth, но Chromium/Firefox не поддерживают это для SOCKS5.

**Наше решение:**
1. Создаем локальный HTTP/SOCKS прокси на `127.0.0.1:random_port`
2. Локальный прокси не требует авторизации
3. Локальный прокси автоматически добавляет credentials к upstream прокси
4. Браузер подключается к локальному прокси
5. Все запросы проксируются через upstream с авторизацией

**Библиотека:** [proxy-chain](https://github.com/apify/proxy-chain)

## 📊 Сравнение с Другими Решениями

### AEZAKMI Pro vs Puppeteer-Extra-Stealth

| Функция | AEZAKMI Pro | Puppeteer-Extra |
|---------|-------------|-----------------|
| Canvas Protection | ✅ Динамический шум | ❌ Базовое скрытие |
| WebGL Protection | ✅ Реалистичные значения | ⚠️ Простая подмена |
| Audio Protection | ✅ Частотный шум | ❌ Нет |
| WebRTC Protection | ✅ Полная блокировка | ⚠️ Частичная |
| Прокси с Auth | ✅ Полная поддержка | ⚠️ Ограничена |
| Fingerprint Generation | ✅ Bayesian Networks | ❌ Случайные значения |
| Firefox Support | ✅ Camoufox | ❌ Нет |

### AEZAKMI Pro vs Selenium-Stealth

| Функция | AEZAKMI Pro | Selenium-Stealth |
|---------|-------------|------------------|
| Движки браузеров | ✅ Chromium, Firefox, Camoufox | ⚠️ Только Chrome |
| Playwright | ✅ Да | ❌ Нет |
| Production Ready | ✅ Да | ⚠️ Для тестирования |
| Прокси Туннель | ✅ Автоматический | ❌ Ручная настройка |
| UI | ✅ Графический интерфейс | ❌ Только код |

## 🐛 Отладка

### Проверка Антидетекта

```javascript
// Откройте консоль в запущенном профиле
console.log('webdriver:', navigator.webdriver); // должно быть undefined
console.log('platform:', navigator.platform);
console.log('hardwareConcurrency:', navigator.hardwareConcurrency);
console.log('deviceMemory:', navigator.deviceMemory);

// Проверка WebGL
const canvas = document.createElement('canvas');
const gl = canvas.getContext('webgl');
const debugInfo = gl.getExtension('WEBGL_debug_renderer_info');
console.log('Vendor:', gl.getParameter(debugInfo.UNMASKED_VENDOR_WEBGL));
console.log('Renderer:', gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL));
```

### Проверка Прокси

```javascript
// Проверка IP
fetch('https://api.ipify.org?format=json')
  .then(r => r.json())
  .then(data => console.log('Your IP:', data.ip));

// Проверка геолокации через IP
fetch('https://ipapi.co/json/')
  .then(r => r.json())
  .then(data => console.log('Location:', data));
```

### Логи Лаунчера

В консоли вы увидите:
```
[LAUNCH] 🚀 Запуск профиля: Профиль 1
[LAUNCH] 🔧 Движок браузера: camoufox
[LAUNCH] 👤 User-Agent: Mozilla/5.0...
[LAUNCH] 🌐 Прокси: socks5://proxy.com:1080
[LAUNCH] 🖥️  Разрешение: 1920x1080
[LAUNCH] 🛡️  Антидетект: ✅✅✅🚫
[LAUNCH] ✅ Профиль "Профиль 1" успешно запущен!
```

## 🎓 Best Practices

### 1. Используйте Camoufox для Критичных Задач
Firefox-based Camoufox обеспечивает лучшую защиту от детекции.

### 2. Меняйте Фингерпринты Регулярно
Создавайте новые профили каждые 7-14 дней для максимальной безопасности.

### 3. Подбирайте Параметры под Задачу
- E-commerce: базовая защита
- Парсинг: средняя защита
- Анонимность: максимальная защита

### 4. Проверяйте Прокси Перед Использованием
Используйте встроенный тест прокси в UI.

### 5. Не Смешивайте Профили
Один профиль = один аккаунт/задача.

## 🆘 Troubleshooting

### Браузер Не Запускается
1. Проверьте логи в консоли
2. Убедитесь, что Node.js установлен
3. Проверьте наличие Playwright браузеров

### Прокси Не Работает
1. Проверьте формат прокси
2. Убедитесь, что прокси доступен
3. Проверьте логи туннеля в консоли

### Canvas/WebGL Детектится
1. Увеличьте `noiseLevel` до 0.5-1.0
2. Переключитесь на Camoufox
3. Проверьте на [pixelscan.net](https://pixelscan.net)

## 📚 Дополнительные Ресурсы

- [Playwright Documentation](https://playwright.dev)
- [Camoufox GitHub](https://github.com/daijro/camoufox)
- [Donut Browser](https://github.com/zhom/donutbrowser)
- [Browser Fingerprinting](https://browserleaks.com)

## 🔮 Roadmap

- [ ] Интеграция Camoufox бинарей
- [ ] Генератор фингерпринтов на основе ML
- [ ] Продвинутое font fingerprinting protection
- [ ] Timezone spoofing с автоопределением
- [ ] Battery API spoofing
- [ ] Permissions API protection

---

**AEZAKMI Pro** - самое продвинутое решение для антидетект-браузера на рынке 🔥
