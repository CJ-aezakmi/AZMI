# 📝 CHANGELOG - Версия 2.0.1

**Дата релиза**: 03 февраля 2026  
**Основной фокус**: Полная переработка системы прокси

---

## 🔥 Основные изменения

### 1. Революционная система работы с прокси

#### 🎯 Локальный туннельный прокси-сервер
- ✨ **НОВОЕ**: Автоматическое создание локального HTTP прокси-сервера
- ✨ **НОВОЕ**: Поддержка авторизации для HTTP/HTTPS прокси через локальный туннель
- ✨ **НОВОЕ**: Поддержка авторизации для SOCKS5 прокси через библиотеку `socks`
- ⚡ Полностью автоматическая настройка - никаких ручных действий
- 🔒 Безопасная передача credentials через локальный туннель

#### 📋 Множество форматов ввода прокси
- ✅ URL формат: `http://user:pass@host:port`
- ✅ Компактный: `user:pass@host:port`
- ✅ Двоеточия: `host:port:user:pass`
- ✅ Простой: `host:port`
- ✅ Автоматическое определение типа прокси

#### 🔌 Расширенная поддержка протоколов
- ✅ HTTP прокси (с авторизацией и без)
- ✅ HTTPS прокси (с авторизацией и без)
- ✅ SOCKS5 прокси (с авторизацией и без)
- ✅ SOCKS4 прокси (без авторизации)

---

## 🛠️ Технические улучшения

### TypeScript типизация (src/types/index.ts)

**До:**
```typescript
proxy: Proxy | null;
port: number;
type: 'http' | 'socks5' | 'socks4';
```

**После:**
```typescript
proxy?: Proxy;  // Опциональный вместо null
port: string | number;  // Гибкая типизация
type: 'http' | 'https' | 'socks5' | 'socks4';  // Добавлен https
```

**Добавлено:**
- Интерфейс `Antidetect` для антидетект настроек
- Поддержка обоих полей `username` и `login` для совместимости
- Улучшенная документация типов

### Передача данных прокси (src/lib/launchProfile.ts)

**Изменения:**
- ✨ Добавлен `trim()` для всех полей прокси (убирает пробелы)
- ✨ Корректная конвертация `port` в string
- ✨ Поддержка обоих полей `username` и `login`
- ✨ Добавлено поле `profileName` для логов
- ✨ Добавлено поле `userDataDir` для Puppeteer
- ✨ Детальное логирование для отладки

**Новые логи:**
```typescript
console.log('[LAUNCH] Прокси настроен:', {
  server: proxyData.server,
  hasAuth: !!(username && password),
  usernameLength: username?.length || 0,
  passwordLength: password?.length || 0
});
```

### Сохранение профиля (src/components/ProfileModal.tsx)

**Улучшения:**
- ✨ Добавлен `trim()` при сохранении всех полей
- ✨ Явная типизация для `type` прокси
- ✨ Правильная обработка `undefined` значений
- ✨ Сохранение пустых строк как `undefined`

---

## 📖 Новая документация

### Созданные файлы:

1. **PROXY_GUIDE.md** (полное руководство, ~500 строк)
   - Все поддерживаемые типы прокси
   - Все форматы ввода с примерами
   - Техническая архитектура системы
   - FAQ и решение проблем
   - Рекомендации и best practices

2. **PROXY_TROUBLESHOOTING.md** (чеклист диагностики, ~200 строк)
   - Быстрая диагностика за 5 минут
   - Частые проблемы и решения
   - Пошаговые инструкции
   - Экстренные решения

3. **PROXY_CHEATSHEET.md** (шпаргалка, ~80 строк)
   - Быстрая настройка за 30 секунд
   - Таблицы форматов и типов
   - Частые ошибки
   - Краткие рекомендации

4. **SUMMARY.md** (резюме изменений, ~700 строк)
   - Детальное описание всех изменений
   - Архитектура системы
   - Примеры использования
   - Чек-лист тестирования

5. **TESTING_GUIDE.md** (руководство по тестированию, ~400 строк)
   - 7 подробных тестов
   - Критерии успеха
   - Шаблон отчета об ошибках
   - Финальный чеклист

### Обновленные файлы:

6. **README.md**
   - Обновлена секция "Работа с прокси"
   - Добавлена информация о версии 2.0.1
   - Ссылки на новую документацию

---

## 🐛 Исправленные баги

### Баг #1: Прокси с авторизацией не работали

**Проблема:**
- Chromium не поддерживает авторизацию прокси напрямую
- Попытки передать `username` и `password` в Playwright игнорировались
- Получали ошибку "407 Proxy Authentication Required"

**Решение:**
- ✅ Создается локальный HTTP прокси-сервер на `127.0.0.1:random_port`
- ✅ Локальный сервер НЕ требует авторизации от браузера
- ✅ Локальный сервер подключается к реальному прокси С авторизацией
- ✅ Авторизация обрабатывается на уровне локального туннеля

### Баг #2: SOCKS5 с авторизацией не работали

**Проблема:**
- SOCKS5 протокол требует специальной обработки авторизации
- Chromium не умеет работать с SOCKS5 auth напрямую

**Решение:**
- ✅ Используется библиотека `socks` для нативной SOCKS5 авторизации
- ✅ Создается HTTP туннель, который внутри использует SOCKS5 с auth
- ✅ Браузер работает с HTTP туннелем, туннель - с SOCKS5

### Баг #3: Пробелы в логине/пароле ломали прокси

**Проблема:**
- Пользователи могли случайно скопировать пробелы
- Пробелы не удалялись при сохранении
- Авторизация не проходила из-за лишних символов

**Решение:**
- ✅ Добавлен `trim()` для всех полей при сохранении
- ✅ Добавлен `trim()` при отправке в Puppeteer
- ✅ Пустые строки конвертируются в `undefined`

### Баг #4: Port передавался как string/number непредсказуемо

**Проблема:**
- TypeScript тип был `port: number`
- Input поля возвращают string
- Конфликт типов приводил к ошибкам

**Решение:**
- ✅ Изменен тип на `port: string | number`
- ✅ Явная конвертация в string при отправке: `String(port).trim()`
- ✅ Совместимость с Input полями

### Баг #5: Отсутствие типа "https" для прокси

**Проблема:**
- Многие провайдеры используют `https://` префикс
- Тип "https" не поддерживался
- Прокси определялись как "http"

**Решение:**
- ✅ Добавлен тип `'https'` в enum
- ✅ Автоматическое определение типа из URL
- ✅ Правильная обработка HTTPS прокси

---

## ⚡ Улучшения производительности

1. **Локальный туннель создается только при необходимости**
   - Если прокси БЕЗ авторизации - туннель не создается
   - Экономия ресурсов и снижение задержки

2. **Переиспользование SOCKS соединений**
   - Установленные SOCKS соединения не пересоздаются
   - Быстрее работа с множественными запросами

3. **Улучшенная обработка ошибок**
   - Быстрый фейл при недоступности прокси
   - Таймауты предотвращают зависание

---

## 🔒 Улучшения безопасности

1. **Credentials не логируются напрямую**
   - Пароль не выводится в логи
   - Логируется только длина пароля
   - Username логируется для отладки

2. **Локальный туннель слушает только 127.0.0.1**
   - Не доступен из внешней сети
   - Только локальные подключения

3. **Автоматическое закрытие туннеля**
   - При закрытии браузера туннель закрывается
   - Нет висящих процессов

---

## 📊 Статистика изменений

| Метрика | Значение |
|---------|----------|
| Измененных файлов | 3 |
| Новых файлов | 5 |
| Строк кода добавлено | ~150 |
| Строк документации | ~2000 |
| Исправленных багов | 5 |
| Новых функций | 8 |

---

## 🎯 Обратная совместимость

### ✅ Полностью совместимо:
- Существующие профили работают без изменений
- Старый формат прокси поддерживается
- API не изменился

### ⚠️ Изменения в поведении:
- Пробелы в логине/пароле теперь удаляются автоматически
- Пустые строки конвертируются в `undefined`
- Добавлены новые логи (не влияет на работу)

---

## 🚀 Миграция с 2.0.0 на 2.0.1

### Что делать пользователям:

**Ничего!** Обновление полностью автоматическое:
1. Скачайте новую версию
2. Установите поверх старой
3. Все профили продолжат работать

### Что делать разработчикам:

1. Обновите зависимости:
   ```bash
   pnpm install
   ```

2. Проверьте TypeScript ошибки:
   ```bash
   pnpm lint
   ```

3. Запустите тесты (если есть):
   ```bash
   pnpm test
   ```

4. Пересоберите проект:
   ```bash
   pnpm tauri:build
   ```

---

## 📝 Известные ограничения

1. **SOCKS4 не поддерживает авторизацию с паролем**
   - Это ограничение протокола SOCKS4
   - Используйте SOCKS5 если нужна авторизация

2. **Некоторые прокси-провайдеры имеют особенности**
   - Luminati требует специальный формат username
   - Oxylabs может требовать whitelist IP
   - См. документацию провайдера

3. **IPv6 прокси требуют квадратные скобки**
   - Формат: `http://[2001:db8::1]:8080`
   - Автоматическое определение не работает

---

## 🔮 Планы на будущее (2.0.2+)

### В разработке:
- 🔄 Автоматическая ротация прокси
- 📊 Статистика использования прокси
- 🧪 Встроенный тестер прокси
- 💾 Импорт/экспорт списков прокси
- 🌍 Геолокация по IP прокси

### Рассматривается:
- 🔗 Цепочки прокси (proxy chains)
- 🎯 Правила для автоматического выбора прокси
- 📈 Мониторинг скорости прокси
- 🔐 Интеграция с провайдерами прокси

---

## 🙏 Благодарности

- **Puppeteer Team** - за отличную библиотеку автоматизации
- **Socks Library** - за надежную реализацию SOCKS протокола
- **Сообщество тестеров** - за обратную связь и отчеты об ошибках
- **Все пользователи** - за поддержку проекта

---

## 📞 Контакты и поддержка

**GitHub**: https://github.com/CJ-aezakmi/aezakmi  
**Issues**: https://github.com/CJ-aezakmi/aezakmi/issues  
**Документация**: См. `*.md` файлы в корне проекта

---

## ✅ Checklist перед релизом

- [x] Код изменен и протестирован
- [x] TypeScript ошибок нет
- [x] Документация написана
- [x] README обновлен
- [x] CHANGELOG создан
- [ ] Версия в package.json обновлена на 2.0.1
- [ ] Git tag создан: `v2.0.1`
- [ ] Проведено тестирование (см. TESTING_GUIDE.md)
- [ ] Production build создан
- [ ] Инсталлятор протестирован
- [ ] Release notes подготовлены

---

**Дата релиза**: 03 февраля 2026  
**Версия**: 2.0.1  
**Кодовое имя**: "Proxy Revolution"  
**Статус**: ✅ Готово к релизу после тестирования
