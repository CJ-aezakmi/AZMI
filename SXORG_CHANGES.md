# Изменения SX.ORG интеграции

## Что было исправлено:

### 1. ✅ Исправлено отображение прокси
- **Проблема**: Прокси отображались с неправильными именами и статусом "без авторизации"
- **Решение**: Обновлена структура данных для соответствия реальному API SX.ORG
  - Изменено: `host` → `server`
  - Изменено: `username` → `login`
  - Добавлено: автоматическое определение типа прокси (HTTP/SOCKS5)
  - Добавлено: сохранение метаданных (refresh_link, геолокация)

### 2. ✅ Добавлен импорт существующих прокси
- **Проблема**: На вкладке "Импортировать прокси" ничего не отображалось
- **Решение**: 
  - Реализована загрузка прокси из API SX.ORG
  - Добавлен поиск по названию, IP и геолокации
  - Возможность массового выбора и импорта
  - Кнопка обновления списка

### 3. ✅ Удален выбор типа авторизации
- **Проблема**: Лишний интерфейс выбора Shared/Dedicated-порта
- **Решение**: Полностью убрана секция выбора типа авторизации (используется только Shared)

### 4. ✅ Добавлена кнопка обновления IP
- **Проблема**: Не было возможности обновить IP прокси
- **Решение**:
  - Добавлена кнопка с иконкой 🔄 для каждого SX.ORG прокси
  - Кнопка отображается только для прокси с поддержкой refresh_link
  - Показывает анимацию загрузки во время обновления
  - Доступна в двух местах:
    - В списке прокси (Dashboard → Прокси)
    - В окне импорта SX.ORG

### 5. ✅ Настройки ротации (TTL) работают
- **Проблема**: Не было ясно, работают ли настройки времени ротации
- **Решение**: 
  - TTL правильно передается в API
  - Для режима "Rotate" используется заданный интервал
  - Для режимов "Keep" и "Break" устанавливается минимум 1 минута

## Как использовать:

### Создание новых прокси:
1. Откройте SX.ORG интеграцию
2. Перейдите на вкладку "Создать прокси"
3. Выберите страну → регион → город
4. На вкладке "Расширенные настройки":
   - Задайте имя (опционально)
   - Количество прокси
   - Поведение: Keep/Break/Rotate
   - Если Rotate: укажите интервал ротации в минутах

### Импорт существующих прокси:
1. Откройте SX.ORG интеграцию
2. Перейдите на вкладку "Импортировать прокси"
3. Используйте поиск при необходимости
4. Выберите нужные прокси (чекбоксы)
5. Нажмите "Импортировать"

### Обновление IP адреса:
1. Найдите прокси в списке (Dashboard → Прокси)
2. Нажмите кнопку 🔄 рядом с прокси
3. Дождитесь подтверждения обновления

## Технические детали:

### Измененные файлы:
- `src/lib/sxorg-api.ts` - обновлен интерфейс `SXOrgProxyPort`
- `src/components/SXOrgIntegration.tsx` - исправлена конвертация прокси
- `src/components/SXOrgCreateProxy.tsx` - убран выбор типа авторизации, исправлен TTL
- `src/components/SXOrgImportProxy.tsx` - добавлена кнопка refresh, исправлены поля
- `src/pages/Dashboard.tsx` - добавлена функция обновления IP
- `src/types/index.ts` - добавлено поле metadata в интерфейс Proxy

### Структура API response (подтверждена тестами):
```json
{
  "id": 150675315,
  "server": "89.39.104.79",
  "port": 19700,
  "login": "01kgjsy5dwvd...",
  "password": "0pKIGAbUsysLbN3e",
  "country_code": "US",
  "country": "United States",
  "proxy_type_id": 1,
  "type_id": 2,
  "refresh_link": "https://api.sx.org/v2/proxy/refresh/..."
}
```

## Проверенные функции:
- ✅ Создание прокси (работает, тестовый ID: 150675315)
- ✅ Правильное отображение в списке
- ✅ Импорт существующих прокси
- ✅ Обновление IP через refresh_link
- ✅ Передача TTL для ротации
- ✅ Определение типа прокси (HTTP/SOCKS5)
