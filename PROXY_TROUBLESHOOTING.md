# ✅ Чеклист: Профили с прокси не работают

## 🔍 Быстрая диагностика

### Шаг 1: Проверка формата прокси (2 минуты)

```
✅ Правильные форматы:
   • http://user:pass@123.45.67.89:8080
   • socks5://user:pass@proxy.com:1080
   • user:pass@123.45.67.89:8080
   • 123.45.67.89:8080:user:pass

❌ Неправильные форматы:
   • 123.45.67.89:8080 user:pass (с пробелами)
   • http//user:pass@... (без :)
   • user@pass@host:port (лишний @)
```

**Действие**: Если формат неправильный - исправьте через "Редактировать профиль"

---

### Шаг 2: Проверка заполнения полей (1 минута)

Откройте профиль → вкладка "Прокси":

```
✅ Должно быть:
   [x] Использовать прокси
   Тип: [HTTP/HTTPS/SOCKS5]
   Хост: [заполнено]
   Порт: [заполнено]
   Логин: [может быть пустым]
   Пароль: [может быть пустым]

❌ Частые ошибки:
   [ ] Использовать прокси - НЕ ВКЛЮЧЕНО!
   Хост: [пустое поле]
   Порт: [пустое поле]
```

**Действие**: Убедитесь что чекбокс "Использовать прокси" ВКЛЮЧЕН

---

### Шаг 3: Проверка типа прокси (30 секунд)

```
Если прокси начинается с:
   http://    → выберите тип HTTP
   https://   → выберите тип HTTPS
   socks5://  → выберите тип SOCKS5
   socks4://  → выберите тип SOCKS4
   
Если нет префикса:
   → выберите HTTP (по умолчанию)
```

**Действие**: Убедитесь что тип соответствует протоколу

---

### Шаг 4: Проверка логов (3 минуты)

**Windows:**
1. Запустите профиль
2. Откройте диспетчер задач → найдите `node.exe`
3. Если `node.exe` НЕТ → проблема с запуском скрипта
4. Если `node.exe` ЕСТЬ → проверьте что браузер открылся

**Логи в консоли (dev режим):**
```
[LAUNCH] Запуск профиля: ...
[LAUNCH] Прокси enabled: true   ← Должно быть true!
[PROD] ✓ Процесс запущен         ← Должно быть
[PUPPETEER] 🔧 Настройка прокси  ← Должно быть
[TUNNEL] ✅ Локальный прокси     ← Для прокси с авторизацией
[PUPPETEER] ✅ Браузер запущен   ← Должно быть
```

**Действие**: Если нет строки `[LAUNCH] Прокси enabled: true` - прокси не включен!

---

### Шаг 5: Проверка авторизации (2 минуты)

**Если прокси требует логин/пароль:**

```
✅ Правильно:
   Логин: myuser
   Пароль: mypass123
   
❌ Неправильно:
   Логин:  myuser  (пробелы!)
   Пароль: my pass (пробел в пароле!)
   Логин: (пустое)
   Пароль: (пустое) ← но прокси требует авторизацию!
```

**Действие**: Убедитесь что логин/пароль указаны БЕЗ пробелов

---

### Шаг 6: Тест прокси (1 минута)

Перейдите в раздел **"Прокси"** (боковое меню):

1. Найдите ваш прокси в списке
2. Нажмите "Проверить"
3. Статус должен стать "✅ Работает"

**Если статус "❌ Не работает":**
- Прокси действительно не работает
- Неправильный логин/пароль
- Прокси заблокирован

**Действие**: Свяжитесь с провайдером прокси или используйте другой

---

## 🚨 Частые проблемы и решения

### Проблема #1: "Прокси enabled: false"

**Причина**: Чекбокс "Использовать прокси" не включен

**Решение**:
1. Откройте профиль → "Редактировать"
2. Вкладка "Прокси"
3. Поставьте галочку ✅ "Использовать прокси"
4. Сохраните

---

### Проблема #2: "407 Proxy Authentication Required"

**Причина**: Неправильный логин или пароль

**Решение**:
1. Проверьте логин/пароль у провайдера
2. Убедитесь что НЕТ пробелов
3. Попробуйте скопировать через Ctrl+C / Ctrl+V
4. Если пароль содержит спецсимволы (@:/) - используйте URL encoding

---

### Проблема #3: Браузер открывается но не загружает сайты

**Причина**: Прокси не отвечает или блокирует соединения

**Решение**:
1. Проверьте прокси через "Прокси" → "Проверить"
2. Попробуйте другой тип прокси (SOCKS5 вместо HTTP)
3. Свяжитесь с провайдером прокси
4. Попробуйте временно БЕЗ прокси

---

### Проблема #4: "SOCKS Authentication failed"

**Причина**: SOCKS5 сервер не поддерживает авторизацию

**Решение**:
1. Убедитесь что это действительно SOCKS5 (не SOCKS4)
2. Проверьте что логин/пароль верные
3. Некоторые SOCKS5 работают БЕЗ авторизации - попробуйте оставить логин/пароль пустыми

---

### Проблема #5: Показывает мой реальный IP на whoer.net

**Причина**: Прокси не применяется к браузеру

**Решение**:
1. Проверьте что "Использовать прокси" ВКЛЮЧЕНО
2. Проверьте логи - должно быть `[PUPPETEER] 🔧 Настройка прокси`
3. Пересоздайте профиль с прокси заново
4. Перезапустите приложение

---

## ⚡ Экстренное решение

**Если ничего не помогает:**

1. **Создайте новый профиль** с нуля
2. **Используйте "Быстрый ввод"** на вкладке "Прокси"
3. **Вставьте прокси в формате**: `http://user:pass@host:port`
4. **Нажмите "Применить"** и сразу **"Сохранить"**
5. **Запустите профиль**

**Если всё равно не работает:**
- Прокси действительно не работает
- Проверьте через другой инструмент (например, Postman)
- Свяжитесь с провайдером прокси

---

## 📞 Нужна помощь?

1. Откройте консоль разработчика (F12)
2. Скопируйте ВСЕ логи
3. Создайте Issue на GitHub
4. Приложите логи и описание проблемы

**Важно**: НЕ публикуйте логин/пароль от прокси в публичных местах!

---

**Версия чеклиста**: 1.0  
**Дата**: 03.02.2026  
**Применимо к**: AEZAKMI Pro 2.0.0+
