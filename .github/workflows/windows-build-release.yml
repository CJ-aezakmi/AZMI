name: Build & Release (Windows)

on:
  push:
    branches: [ main ]
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-windows:
    name: Build AEZAKMI Pro
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build web assets
        run: pnpm build

      - name: Setup Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable-x86_64-pc-windows-msvc
          profile: minimal
          override: true

      - name: Build Tauri app (Windows)
        env:
          CARGO_HOME: ${{ runner.temp }}/cargo
        run: pnpm tauri build --target x86_64-pc-windows-msvc

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: aezakmi-installer
          path: |
            src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe

      - name: Create GitHub Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: ncipollo/release-action@v1
        with:
          artifacts: |
            src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
          token: ${{ secrets.GITHUB_TOKEN }}
          draft: false
          prerelease: false
          allowUpdates: true
          replacesArtifacts: true
          body: |
            # AEZAKMI Pro v2.0.0 - Windows Edition
            
            ## 📦 Установка
            
            1. Скачайте **AEZAKMI_2.0.0_x64-setup.exe**
            2. Запустите установщик
            3. При первом запуске приложение автоматически установит:
               - Node.js 20 Portable
               - Playwright + Chromium
            4. Дождитесь завершения установки (~5-10 минут)
            5. Приложение готово к работе!
            
            ## 🔑 Лицензионные ключи
            
            Используйте один из ключей из `LICENSE_KEYS.md`
            
            ---
            
            **Примечание:** Первый запуск занимает 5-10 минут для установки зависимостей.
