# Решение проблемы: Профили не запускаются в продакшн версии

## Проблема
**Симптомы:**
- В dev режиме (`pnpm tauri:dev`) всё работает отлично
- В продакшн версии (после сборки `pnpm tauri build`) профили не запускаются

**Причина:**
В режиме разработки приложение использует Playwright из `node_modules` проекта, но при сборке продакшн версии Playwright не копируется в установочный пакет.

## Решение

### 1. Изменён скрипт запуска (`scripts/launch_playwright.cjs`)
**Что сделано:**
- Добавлена логика поиска локального Playwright в `node_modules` рядом с exe файлом
- Скрипт пытается подключить локальный Playwright, если он есть
- Если локального нет - использует глобальный
- Добавлено подробное логирование с префиксами `[SCRIPT]`

**Код:**
```javascript
const appDir = process.cwd();
const localPlaywrightPath = path.join(appDir, 'node_modules', 'playwright');

if (fs.existsSync(localPlaywrightPath)) {
  console.log('[SCRIPT] Найден локальный Playwright');
  chromium = require(path.join(localPlaywrightPath, 'index.js')).chromium;
} else {
  console.log('[SCRIPT] Локальный Playwright не найден, используем глобальный');
  chromium = require('playwright').chromium;
}
```

### 2. Добавлена команда установки Playwright (`src-tauri/src/lib.rs`)
**Что сделано:**
- Создана Tauri команда `check_and_install_playwright`
- Устанавливает Playwright локально в папку приложения
- Создаёт `package.json` если его нет
- Устанавливает браузеры Chromium

**Код:**
```rust
#[tauri::command]
async fn check_and_install_playwright() -> Result<String, String> {
    let app_dir = std::env::current_exe()
        .ok()
        .and_then(|p| p.parent().map(|p| p.to_path_buf()))
        .ok_or("Failed to get app directory")?;
    
    // Проверяем, установлен ли Playwright локально
    let playwright_dir = app_dir.join("node_modules").join("playwright");
    
    if playwright_dir.exists() {
        return Ok("Playwright уже установлен локально".to_string());
    }
    
    // Устанавливаем Playwright
    Command::new("npm")
        .arg("install")
        .arg("playwright")
        .current_dir(&app_dir)
        .output();
    
    // Устанавливаем браузеры
    Command::new("npx")
        .arg("playwright")
        .arg("install")
        .arg("chromium")
        .current_dir(&app_dir)
        .output();
    
    Ok("Playwright успешно установлен!".to_string())
}
```

### 3. Добавлена кнопка в UI (`src/pages/Dashboard.tsx`)
**Что сделано:**
- Добавлена функция `handleInstallPlaywright`
- Добавлена кнопка "Установить Playwright" в сайдбар
- Показываются тосты с прогрессом установки

**UI:**
```tsx
<Button
  onClick={handleInstallPlaywright}
  variant="outline"
  className="w-full text-xs"
>
  <Download className="w-3 h-3 mr-1" />
  Установить Playwright
</Button>
```

### 4. Обновлена документация
**Что сделано:**
- Обновлён `README.md` с инструкциями по первому запуску
- Создан `ПЕРВЫЙ_ЗАПУСК.md` с детальной инструкцией
- Добавлено требование Node.js 18+

## Инструкция для пользователя

### При первом запуске продакшн версии:

1. **Установите Node.js 18+**
   - Скачайте с https://nodejs.org/
   - Выберите LTS версию

2. **Активируйте лицензию**
   - Введите лицензионный ключ
   - Нажмите "Активировать"

3. **Установите Playwright**
   - Нажмите кнопку "Установить Playwright" в сайдбаре
   - Дождитесь завершения (2-5 минут)

4. **Проверьте готовность**
   - Нажмите "🔍 Диагностика"
   - Убедитесь, что всё готово

5. **Запустите профиль**
   - Создайте профиль
   - Нажмите Play

## Техническая информация

### Почему так?
- **Dev режим**: Playwright в `node_modules` проекта, доступен из `process.cwd()`
- **Production**: Приложение устанавливается в `C:\Program Files\`, `node_modules` нет
- **Решение**: Локальная установка Playwright в папку приложения

### Альтернативные подходы (не использованы):
1. ❌ Упаковка Playwright в бандл - слишком большой размер (400+ MB)
2. ❌ Глобальная установка - конфликты версий, требует прав администратора
3. ✅ Локальная установка по требованию - оптимально

## Тестирование

### Dev режим:
```bash
pnpm tauri:dev
# Должно работать как раньше
```

### Production:
```bash
pnpm tauri build
# Установить exe
# Нажать "Установить Playwright"
# Запустить профиль
```

## Файлы, которые были изменены:
1. `scripts/launch_playwright.cjs` - логика поиска локального Playwright
2. `src-tauri/src/lib.rs` - команда установки Playwright
3. `src/pages/Dashboard.tsx` - кнопка установки в UI
4. `README.md` - обновлённая документация
5. `ПЕРВЫЙ_ЗАПУСК.md` - новая инструкция

## Дата исправления
14 декабря 2025

## Статус
✅ Исправлено и готово к тестированию
